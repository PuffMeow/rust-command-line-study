[package]
edition = "2021"
name = "catr"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = "2.33"

[dev-dependencies]
assert_cmd = "2" # 用于命令行工具的测试
predicates = "2" # 断言判断
rand = "0.8" # 随机数创建
