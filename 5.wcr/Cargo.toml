[package]
edition = "2021"
name = "wcr"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
assert_cmd = "2"
clap = "2.33"
predicates = "2"
rand = "0.8"
