[package]
edition = "2021"
name = "echor"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = "2.33" # command-line arguments parser

[dev-dependencies]
assert_cmd = "2"
predicates = "2"
